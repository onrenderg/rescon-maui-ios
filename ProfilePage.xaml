<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ResillentConstruction.ProfilePage"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls" 
             ios:Page.UseSafeArea="true">

    <NavigationPage.TitleView>
        <Grid x:Name="Custom_Navigation">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="ic_himachallogo.png" HeightRequest="40" WidthRequest="40" Grid.Column="0" VerticalOptions="Center"/>
            <Grid Grid.Column="1" VerticalOptions="Center" HorizontalOptions="Center">
                <Label x:Name="lbl_navigation_header"  FontSize="20" FontAttributes="Bold" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
            </Grid>
        </Grid>
    </NavigationPage.TitleView>
   
    <ContentPage.ToolbarItems>
    <ToolbarItem Clicked="ToolbarItem_Clicked">
        <ToolbarItem.IconImageSource>
            <AppThemeBinding Light="language_ico.png" Dark="white_language_ico.png" />
        </ToolbarItem.IconImageSource>
    </ToolbarItem>
    </ContentPage.ToolbarItems>

    <Grid>
        <Grid Margin="5" RowSpacing="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label x:Name="lbl_user_header1" Text="Profile" Padding="3" FontSize="18" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Margin="5" FontAttributes="Bold" TextColor="White" BackgroundColor="Navy" Grid.Row="0"/>
            <Label x:Name="lbl_mandatory" Text="*Mandatory Fields" VerticalOptions="Center" FontSize="14" FontAttributes="Bold" HorizontalTextAlignment="Center" Grid.Row="1"/>
            <Label x:Name="lbl_gpsdistrict" Text="District Name" VerticalOptions="Center" FontSize="18" FontAttributes="Bold" HorizontalTextAlignment="Center" Grid.Row="2"/>

            <ScrollView Grid.Row="3">
                <Border Stroke="Navy" StrokeThickness="1" BackgroundColor="Transparent" Margin="5" Padding="7">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10"/>
                    </Border.StrokeShape>
                    <Grid RowSpacing="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Label x:Name="lbl_name" Text="Name *" FontSize="20" FontAttributes="Bold" Grid.Row="0"/>
                        <Entry Placeholder="Enter Name" x:Name="entry_name" FontSize="20" MaxLength="50" Keyboard="Text" Grid.Row="1"/>

                        <Label x:Name="lbl_mobile" Text="Mobile *" FontSize="20" FontAttributes="Bold" Grid.Row="2"/>
                        <Entry x:Name="entry_mobile" FontSize="20" MaxLength="10" Keyboard="Numeric" Grid.Row="3"/>

                        <Label x:Name="lbl_email" Text="Email *" FontSize="20" FontAttributes="Bold" Grid.Row="4"/>
                        <Entry x:Name="entry_email" FontSize="20" MaxLength="50" Keyboard="Email" Grid.Row="5"/>

                        <Label x:Name="lbl_District" Text="District *" FontSize="20" FontAttributes="Bold" Grid.Row="6"/>
                        <Grid ColumnDefinitions="*,Auto" Grid.Row="7">
                            <Picker Title="Select District" x:Name="Picker_District" SelectedIndexChanged="Picker_District_SelectedIndexChanged" Grid.Column="0" FontSize="20" />
                            <Button x:Name="Btn_getdistrict"
                            ImageSource="location_searching.png"
                            Clicked="Btn_getdistrict_Clicked"
                            BackgroundColor="Navy"
                            TextColor="White"
                            Grid.Column="1"
                            IsVisible="False"
                            ContentLayout="Left,10" />
                        </Grid>

                        <Label x:Name="lbl_fallinzone" Text="You fall in Zone : " FontSize="20" HorizontalOptions="Center" FontAttributes="Bold" Grid.Row="8"/>

                        <Label x:Name="lbl_place" Text="Enter Place Of construction *" FontSize="20" FontAttributes="Bold" Grid.Row="9"/>
                        <Entry x:Name="entry_place" Placeholder="Enter Place Of construction" FontSize="20" MaxLength="50" Keyboard="Text" Grid.Row="10"/>

                    </Grid>

                </Border>
            </ScrollView>

            <Grid Grid.Row="4" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Text="Cancel" x:Name="Btn_cancel" Clicked="Btn_cancel_Clicked" BackgroundColor="Silver" TextColor="Black" FontSize="18" FontAttributes="Bold"/>
                <Button Grid.Column="1" Text="Save" x:Name="Btn_save" Clicked="Btn_save_Clicked" BackgroundColor="Navy" TextColor="White" FontSize="18" FontAttributes="Bold"/>
            </Grid>
        </Grid>

        <ContentView x:Name="Loading_activity" BackgroundColor="#C0808080" Padding="20, 20" IsVisible="false" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">
            <Grid VerticalOptions="Center" HorizontalOptions="Center">
                <Border BackgroundColor="White" Padding="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="5"/>
                    </Border.StrokeShape>
                    <Grid Margin="6" ColumnSpacing="10" Padding="4,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ActivityIndicator IsVisible="true" IsRunning="true" Margin="5" Grid.Column="0"/>
                        <Label x:Name="lbl_PleaseWait"
                               Text="Resolving District as per your location ..."
                               TextColor="Black"
                               FontAttributes="Bold"
                               VerticalTextAlignment="Center"
                               LineBreakMode="WordWrap"
                               MaxLines="3"
                               Grid.Column="1"/>
                    </Grid>
                </Border>
            </Grid>
        </ContentView>
    </Grid>
</ContentPage>